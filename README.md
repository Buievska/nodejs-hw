Notes API

REST API для роботи з нотатками (Node.js + Express + MongoDB).
Проєкт реалізовано з використанням Mongoose, Pino, CORS і dotenv.

🚀 Як запустити

1. Клонувати репозиторій

2. Встановити залежності

3. Створити файл .env
   PORT=
   MONGO_URL=

4. Запустити сервер

🔹 Маршрути API
Метод Маршрут Опис
GET /notes Отримати всі нотатки
GET /notes/:noteId Отримати нотатку за ID
POST /notes Створити нову нотатку
PATCH /notes/:noteId Оновити нотатку
DELETE /notes/:noteId Видалити нотатку
GET / Перевірка, чи API працює
